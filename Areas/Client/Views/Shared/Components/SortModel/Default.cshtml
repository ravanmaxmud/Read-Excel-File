@using Read_Excel_File.Areas.Client.ViewModels.Home
@using Read_Excel_File.Areas.Client.ViewModels.Models
@model ListModelViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@*@foreach (var item in Model)
{
    <div class="col-lg-3">
        <div class="card mb-3" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">  @item.Name</h5>

                <div class="wrap-num-product flex-w m-r-20 m-tb-10">
                    <div class="btn-num-product-down btn-minus cl8 hov-btn3 trans-04 flex-c-m">
                        <i class="fs-16 zmdi zmdi-minus"></i>
                    </div>

                    <input class="mtext-104 cl3 txt-center num-product" type="number" name="num-product" value="1">

                    <div class="btn-num-product-up btn-plus cl8 hov-btn3 trans-04 flex-c-m">
                        <i class="fs-16 zmdi zmdi-plus"></i>
                    </div>
                </div>
                <a asp-route="client-basket-add" asp-route-modelName="@item.Name" class="btn btn-primary add-product-to-basket-single-btn">Add</a>
            </div>
        </div>
    </div>
}*@
<select asp-for="ModelId" class="form-control"
        asp-items="@(new SelectList(Model.Models, nameof(ModelListItemViewModel.Id), nameof(ModelListItemViewModel.Name)))" class="form-select"> </select>
<span asp-validation-for="Models" class="text-danger"></span>
<input class="mtext-104 cl3 txt-center num-product" type="number" name="num-product" value="1">
<a asp-route="client-basket-add" asp-route-modelName="" class="btn btn-danger btnAdd">+</a>
<div  class="selectDiv">

</div>

